import React from 'react';

// AdminDiagnostics: removed visible diagnostic UI.
// Kept as a stub so imports remain valid elsewhere in the app.
export const AdminDiagnostics: React.FC = () => null;

export default AdminDiagnostics;
